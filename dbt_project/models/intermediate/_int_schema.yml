version: 2

models:
  - name: int_flights_enriched
    columns:
      - name: flight_id
        tests: [not_null, unique]
      - name: origin_obs_time_utc
        tests:
          - test_no_future_leakage:
              event_ts_column: scheduled_departure_utc
